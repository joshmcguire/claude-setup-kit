#!/usr/bin/env bash
# SessionStart hook: print the command/skill cheat sheet so every session starts knowing
# what is available. EDIT THIS PER MACHINE — the list below is an example; replace it with
# whatever custom commands and skill packs this machine actually has.

cat <<'EOF'
Available commands — run /toolkit anytime to see this list.

CUSTOM: /research <query> · /toolkit
EOF
